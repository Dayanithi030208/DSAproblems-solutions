class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        //  use hashmap count the frequency and traverse it and add the value in to the array
        HashMap<Integer,Integer> map=new HashMap<>();
        int length=nums.length;
        for(int i=0;i<length;i++){
            map.put(nums[i],map.getOrDefault(nums[i],0)+1);
        }
        ArrayList<HashMap.Entry<Integer,Integer>> list=new ArrayList<>();
        Collections.sort(list, (a, b) -> b.getValue() - a.getValue());
        int top=k;
        ArrayList<Integer> ans=new ArrayList<>(map.entrySet());
        for(int i=0;i<list.size();i++){
            top--;
            ans.add(list.get(i));
        }
        int[] arr=list.stream().mpaToInt(Integer::intValue).toArray();
        return arr;
    }
}
